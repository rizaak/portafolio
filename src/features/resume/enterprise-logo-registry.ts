import type { SimpleIcon } from "simple-icons";
import { siAirbnb } from "simple-icons";

/**
 * Airbnb — official Simple Icon path (see enterprise-logo-assets for remote URLs).
 */
export const SIMPLE_ICON_BY_CLIENT_ID: Partial<Record<string, SimpleIcon>> = {
  airbnb: siAirbnb,
};
